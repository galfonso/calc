<h1>Listing pesajes</h1>

<table>
  <tr>
    <th>Peso</th>
    <th>Comidasconproteinas</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pesajes.each do |pesaje| %>
  <tr>
    <td><%= pesaje.peso %></td>
    <td><%= pesaje.ComidasConProteinas %></td>
    <td><%= link_to 'Show', pesaje %></td>
    <td><%= link_to 'Edit', edit_pesaje_path(pesaje) %></td>
    <td><%= link_to 'Destroy', pesaje, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pesaje', new_pesaje_path %>
